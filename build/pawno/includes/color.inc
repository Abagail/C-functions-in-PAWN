// C++ functions in PAWN plugin by Abagail
// Filename: color.inc
// Purpose: Include - contains the plugin's native list for use in pawno
native SetConsoleColor(color[]);
native systemEx(cmd[]);
native getDirectory(directory[]);
native cleanConsole();
